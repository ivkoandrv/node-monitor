import fiboAsync from "@/services/analyze/fibonacci/fiboAsync";
import fiboChild from "@/services/analyze/fibonacci/fiboAsync";
import fiboWorker from "@/services/analyze/fibonacci/fiboAsync";

// TODO: blocking fibo

export default {
    fiboAsync,
    fiboChild,
    fiboWorker,
}